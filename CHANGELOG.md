# Changelog

## Unreleased

### 统一未设置堆叠上限的物品

- 将 InfX 普通食物、面粉、凝胶球、刷怪蛋、Catalog 原料、Catalog 箭以及原版骨头的最大堆叠数量统一设为 16。

### 恢复地下世界自然装饰

- 地下世界新生成区块现在会进行 70 次 MITE 风格液体源尝试：候选高度保留原始的低层偏置，且只有顶底为石头、四侧恰有三块石头和一格空气的洞穴缝隙才会生成水或熔岩。
- 每个地下世界装饰区块保留一次 `1/4` 概率的棕色蘑菇候选，候选坐标采用原始区块随机坐标加 `8`，高度为 `Y=120` 至 `247`。
- 新增按世界种子稳定的菌丝 post 场：每个候选区块以 `1/16` 概率产生一个 post，扫描周边 `5×5` 候选区块并在半径 24 格内生成菌丝和少量棕色蘑菇；实现不使用跨区块可变缓存，因此不会因并行生成顺序产生接缝。

### 恢复地下世界地牢装饰

- 地下世界新生成区块现在在地下结构装饰阶段进行 16 次 MITE 风格地牢候选，候选位置为区块内随机坐标加 `8`，高度为 `Y=140` 至 `171`；洞穴入口、地板和顶板仍按原版怪物房规则校验。
- 地牢刷怪笼以 `5/6` 概率生成长眠者、以 `1/6` 概率生成远古骷髅领主，并直接绑定现有的地下世界专属简化战利品表；自然生物表保持为空。
- 删除了此前追加到原版普通地牢箱的全局地下世界战利品修改器，避免其他维度的普通地牢获得地下世界战利品。

### 修正传送门互联

- 普通地下世界门和下界门现在按当前实体坐标与维度坐标比例寻找出口，不再读取或写入玩家级旧返回坐标；符文门的远距目标改为入口坐标叠加符文确定性偏移，避免移动符文门后从下界返回到旧地下世界位置。

### 隔离测试模式服务端权限

- `-Dinfx.testMode=true` 时保留原版 OP、`ops.json`、本地控制台、RCON 与 JSON-RPC；测试客户端只能连接到同为测试模式的服务端。
- 普通模式下玩家不再获得 OP 或 OP 满服绕过，已有 `ops.json` 保留但不生效，新建专用服不再自动生成空 `ops.json`。
- 普通模式下本地终端和 GUI 控制台仅允许受限命令白名单，其他命令不执行；RCON 与 JSON-RPC 不会启动监听器。

### 更新地幔材质

- 地幔方块现在内置并使用用户提供的 `16×48` 三帧动画材质，不再引用原版岩浆材质。

### 调整传送门粒子颜色

- 进入下界的传送门现在发出 MITE 红色粒子；普通地下世界门继续使用紫色粒子。
- 符文门和回出生地门现在发出与其青蓝色门面一致的粒子。

### 更新下界传送门材质

- 进入下界的 `infx:nether_portal` 现在使用所提供的 MITE 红色动画传送门材质；地下世界传送门和返生传送门不受影响。

### 调整深层传送门

- 主世界门框底边位于世界底部起八格内且至少一格底框下方接触基岩时，可在当前主世界约 `Y=-10` 的深度进入地下世界；地下世界同等深度的地幔支撑门框可进入下界。
- 地下世界新生成区块的随机底部边界改为地幔，使其能够自然满足进入下界的门框条件；已生成区块不会重写。
- 地下世界与下界门现在支持宽 `2` 至 `21`、高 `3` 至 `21` 的完整门框；新建出口会继承入口门的宽度、高度与朝向，而不再固定生成 `2x3` 门。

### 恢复地下世界内部基岩层

- 地下世界深板岩基底现在叠加四层按世界种子稳定的 MITE 风格二维噪声基岩，中心从低到高为 `Y=-120`、`-72`、`-16` 和 `99`。
- 第一层保留向下连通的封闭形态，其余三层保留不规则上下边缘；仅替换 `Y=-128` 至 `119` 的深板岩，因此不会改写随机地核、MITE 密度洞穴、低层水或顶部基岩。

### 新增已修改结构清单

- 在 `docs/modified-structures.md` 汇总结构生成门禁、结构集与生物群系资格、生成后处理和结构战利品修改。

### 恢复地下世界 MITE 密度地形与深层地层
### 新增结构生成进度门禁

- 林地府邸现在要求任意一名在线玩家当前经验达到 100,000 点；经验被消费、默认死亡导致经验下降或玩家离线后，若没有其他玩家满足阈值，府邸会重新锁定。
- 海底神殿要求任意玩家进入过下界要塞；六种主世界残破传送门要求任意玩家进入过下界；沉船和搁浅沉船要求任意玩家击杀过守卫者。
- 掠夺者前哨站现在与村庄共享第 60 天且全世界已制作铁级工具的生成条件。

### 移除古城与试炼密室生成

- 新生成的主世界区块不再生成远古城市或试炼密室；原版注册表和已生成区块不受影响。

### 重写地下世界深层地层

- 地下世界的新区块从 `Y=-128` 延伸至 `Y=255`，总高 384 格；`Y=-128` 至 `Y=119` 使用深板岩，底部从 `Y=-128` 起由按位置和世界种子稳定的随机 1 至 5 层 `infx:core` 覆盖。
- `Y=120` 至 `Y=247` 恢复整体上移 120 格的 MITE 旧 Nether 密度地形：正密度生成石头，洞穴在 `Y=120` 至 `Y=143` 填水、在 `Y=144` 至 `Y=247` 保持空气。
- `Y=248` 至 `Y=250` 保持石头；顶部随机 1 至 5 层基岩、空生物表、关闭的矿物/雕刻器/放置特征与旧随机源保持不变。
- 已生成区块不会重写，旧区块与新生成区块的边界可能出现地层断面。

### 添加地下世界随机顶底边界

- 新生成地下世界区块的底部会生成随机 1 至 5 层地核，`Y=0` 始终为地核。
- 顶部会生成随机 1 至 5 层基岩，`Y=255` 始终为基岩；这两种边界均按世界种子与位置稳定生成。
- 已生成区块不会重写。

### 调整地下世界高度

- 地下世界现在从 `Y=0` 延伸到最高可放置方块 `Y=255`（上边界为 `Y=256`），共 256 格高度。

### 统一资源文件前缀

- 将以 mite 或 r196 开头的受跟踪资源文件名统一替换为小写 infx（Minecraft 资源标识符要求小写），并同步资源清单、同步脚本和校验测试。

### 统一 Java 与注册前缀

- 将以 Mite 或 R196 开头的 Java 文件名和类型改为 Infx，并同步引用、注册常量、注册路径、数据生成逻辑和测试资源路径；保留旧 NBT 键以兼容已有存档。

### 恢复主世界人工矿井

- 主世界重新使用原版普通与恶地人工矿井的生物群系分布；地下世界不再生成这两类矿井。
- 该调整仅影响之后新生成的区块，已探索区块不会补建或重写人工矿井。
### 重置地下世界生成基线

- 新生成的地下世界区块不再应用洞穴密度、地表替换、雕刻器、矿物与其他放置特征，也不再生成 MITE 内部基岩层或顶底边界基岩；整个有效高度范围默认填充为石头。
- 地下世界不再被标记为古城、埋藏的宝藏、废弃矿井、古迹废墟或试炼密室的生成生物群系；已生成区块不会被重写。
- 地下世界现在完全清空生物生成表，不再自然生成任何原版或 InfX/MITE 生物。

### 新增结构生成进度框架

- 新增可复用的结构生成规则注册表，支持按结构 ID 或结构标签、维度和可组合的世界进度条件限制新生成区块。
- 村庄现改由该框架控制：主世界第 60 天且世界首次制作铁级工具后才会在后续新生成区块中出现；既有区块不会回填。

### 调整地下世界高度

- 地下世界现在使用 `Y=-128` 至上边界 `Y=192` 的 320 格高度范围，最高可放置方块为 `Y=191`；维度类型与噪声设置会同步使用该范围。

### 重置主世界资源矿物生成

- 主世界的煤、铜、铁、银、金、秘银、红石、钻石、青金石和绿宝石现在使用新的硬核稀缺矿表；银和秘银会按石头、深板岩宿主生成对应矿石。
- 移除了原版资源矿物的生物群系额外生成，并关闭原版铁、铜噪声矿脉；地下地形变种、虫蚀石和地下世界矿物保持不变。
- 新规则只会作用于之后新生成的主世界区块，不会改写已探索区块。

### 修复不可挖掘方块与金属铁砧回收

- 服务端现在会拒绝无法产生破坏进度的起挖输入，使其按空挥处理，并避免错误开启挖掘代谢。
- 工具类型不匹配或采集等级不足的方块也会按空挥处理，不能再开始挖掘。
- 客户端现在会将上述无效方块目标走原版左键空气分支，实际播放一次空挥而非仅取消起挖。
- 金属铁砧恢复可手持回收：保留 128 tick 手挖速度、掉落铁砧物品并保留已有损耗。

### 新增砾石方块

- 新增使用用户提供材质的砾石方块；只有燧石铲及更高采掘等级的铲类工具可以正确挖掘。
- 将砾石方块注册名从 `infx:gravel` 更正为 `infx:sgravel`。

### 调整山地表层生成

- 裸岩山峰和风袭沙砾山丘不再生成原版沙盘或砾石盘，改为生成 `infx:sgravel` 砾石盘。
- 风袭沙砾山丘由噪声地表规则生成的原版砾石也改为 `infx:sgravel`。

### 调整水边地表生成

- 石岸和河流底的原版沙盘、砾石盘及地表规则砾石改为生成 `infx:sgravel`。
- 修复石岸和河流砾石盘与山地特征共享放置对象导致的地物排序环。

### 调整砾石斑块生成

- 裸岩山峰、风袭沙砾山丘、石岸和河流中的原版砾石斑块现在也会生成 `infx:sgravel`。

### 调整经验掉落

- 动物死亡和挖掘煤矿、深层煤矿后不再掉落经验。
### 修复工作台合成进度箭头

- 合成进度箭头左移 1 像素，使白色填充逐像素对齐工作台背景箭头。
- 工作台和背包合成进度现在会在服务端刻之间平滑过渡，长配方不再逐像素停顿跳动。
- 合成完成时进度会短暂显示满格后再清零；暂停和取消仍会准确停住或立即复位。

### 添加深板岩矿物

- 添加银、秘银和艾德曼深板岩矿石及其用户提供的材质。
- 三种深板岩矿石沿用对应石头矿石的采掘等级，暂不加入世界生成。

### 调整巨型吸血蝙蝠体积

- 吸血巨蝠现在缩小为普通吸血蝙蝠的 1.5 倍，仍保留更大的渲染和碰撞体积。

### 更新蝙蝠材质

- 夜翼和吸血蝙蝠现在使用用户提供的 32×32 实体材质。
### 修复玩家背包合成界面

- 玩家背包合成现在显示定时合成进度条。
- 背包合成进度现在沿原版箭头轮廓填充，不再出现放大、越界或变形的白色箭头。
- 定时合成完成后的产物现在通过原版同步入包流程交付，不再只清空进度而客户端看不到物品。

### 修复 MITE 土元素攻城 AI

- 土元素现在使用独立的攻城挖掘目标，按 MITE 的目标支撑方块、上半身遮挡和下半身遮挡顺序选取可破坏方块。
- 连续破坏会保留 MITE 的初始冷却、材质速度、短暂停顿和重新选块逻辑；可到达目标、关闭 `mobGriefing` 或目标失效时会正确取消破坏进度。

### 修复土元素实体继承

- 土元素和黏土元素现在直接使用敌对生物基类，不再继承铁傀儡的村庄守卫、修复和碰撞行为。
- 土元素和黏土元素受击和死亡时恢复播放 MITE 的铁傀儡音效。

### 对齐 MITE R196 蝙蝠

- 普通蝙蝠现在使用 3 点生命的 R196 实体，并替换原版自然生成、刷怪笼替换和环境生成表。
- 蝙蝠恢复 MITE 的环境生物身份、洞穴空气与区块光照生成规则、集群上限，以及正常蝙蝠 0 经验和无掉落。
- 吸血蝙蝠和夜翼现在以视线内的猎物为目标；吸血蝙蝠不会捕食地狱犬，满血冷却期间只攻击玩家，并会触发受伤豹猫反击。
- 夜翼不再施加现代黑暗效果，改为 MITE 的玩家专属屏幕渐暗；银甲覆盖率会降低该效果。

### 修复 R196 GameTest 回归

- 便携方块的手持挖掘速度现在补偿原版的非正确工具 `/100` 分母；保险箱所有者重新保持固定的 128 tick 回收速度，并能正常收回保险箱物品。
- 行为饥饿 GameTest 现在覆盖已移除的自定义无效挖掘拦截：输入继续交由原版处理，同时持续挖掘仍按现有规则结算代谢。
- 村民转换 GameTest 显式使用空手 R196 僵尸，避免装备状态干扰其 MITE 转换前提。
- 女巫诅咒 GameTest 直接验证实体级无敌的生存玩家仍可进入诅咒候选集，不再把 1/4 的游戏内随机施咒概率当作测试前提。
- 骷髅重新评估武器时会停止并移除旧的原版及 R196 弓攻击 goal，避免遗留冷却和重复 goal 抢占当前武器行为。
- 远程攻击距离 GameTest 现在强制动态实体区块保持 ticking，并为骷髅装备头盔，避免区块卸载或明亮世界中的避日寻路干扰弓攻击断言。
- 末影人珍珠拾取 GameTest 现在强制实体区块 ticking、屏蔽同批测试创建的无关玩家目标，并保护测试珍珠不被其他玩家拾取。

### 对齐 MITE 普通刷怪笼光照

- 普通刷怪笼现在忽略火把和其他环境亮度造成的现代黑暗判定，恢复 MITE 1.6 行为。
- 非光照出生条件仍保留；会在露天白天被日光灼伤的实体仍不会由刷怪笼生成。试炼刷怪笼和自定义 SpawnRules 不受影响。
### 调整丛林植被

- 丛林、稀疏丛林和竹林不再自然生成西瓜。

### 修复疾跑

- 营养值耗尽时会立即停止疾跑，剩余饱和度不再允许重新疾跑。
### 修复测试模式世界类型

- test mode 创建和重新载入世界时会采用所选的超平坦等世界类型，不再被巨型生物群系配置覆盖。
- 普通模式仍保持现有世界类型锁定规则。

### 修复材料剪刀

- 材料剪刀现在可正确获得精准采集。
- 右键材料剪刀可直接剪切树叶、羊毛、蓝莓丛等剪刀有效方块。
- 右键剪切方块现在播放剪刀音效，并使蓝莓丛同时掉落丛本体。
- 左键使用材料剪刀不再破坏方块。
- 左键材料剪刀恢复正常近战攻击。
- 副手剪刀不再误用主手武器执行右键攻击。
- 被事件取消的剪刀攻击不再消耗耐久或施加冷却。

### 修复主世界地形

- 移除主世界大型洞穴生成，避免海底出现异常水幕和瀑布。
# InfiniteX 更新日志

## 未发布 - 2026-07-29

- 删除战斗断线生命/生存数据惩罚和非正常断线后的重连时间限制。
- 删除聊天刷屏计分与自动踢出。
- 删除 InfiniteX 自定义无效挖掘输入和采掘合法性拦截，保留 MITE 挖掘速度计算与保险箱所有权保护。
