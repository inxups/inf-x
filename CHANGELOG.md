# Changelog

## Unreleased

### 复刻 MITE 腐蚀音效与复合物品腐蚀

- 方块完成即时或渐进溶解、掉落物实际受损，以及玩家背包或手持物品实际受腐蚀后，播放 MITE 的 `random/fizz` 腐蚀音效；仅进入渐进计时或命中免疫对象时不播放。
- 腐蚀判定补充 MITE 的复合材料规则：工具、弓、箭和钓鱼竿保留木柄/木杆的酸敏感性，剪刀和护甲不额外添加木材；艾德曼对酸免疫。
- 掉落的不可损坏物品使用 MITE 的 5 点腐蚀生命值，连续受到腐蚀后整个掉落实体消失；弓和钓鱼竿使用非工具耐久缩放。
- 酸性史莱姆命中玩家发射的易腐蚀箭矢时禁止箭矢回收并播放腐蚀音效。
### 移植 MITE 助燃/点燃与火焰蔓延

- 为原版木材、原木、树叶、书架、TNT、草木、蛛网、作物、羊毛、藤蔓、煤炭块、干草块、甘蔗和灌木接入 MITE 的 `chanceToEncourageFire / abilityToCatchFire` 数值；干草块修正为 `60/100`，灌木修正为 `30/50`。
- 火焰更新现在使用 MITE 的 `0..15` 年龄、30 至 39 tick 调度、每次三分之一概率增长、无邻近可燃物时按年龄熄灭，以及地狱岩/末地基岩永久火规则。
- 恢复高湿度对六向点燃分母（水平 300、垂直 250）和空气蔓延概率的影响；空气蔓延扫描 `x/z ±1、y -1..+4`，按助燃值、难度、年龄和高度计算概率，并保留 TNT 点燃、雨水熄灭和新火年龄继承。

### 同步 MITE 月相时间线与天空渲染

- 数据生成现在覆盖 `minecraft:moon` 时间线：第 1 天从 MITE 对应的 `waning_gibbous` 开始，第 8 天为满月，并让地表史莱姆亮度轨与月面阶段同步。
- 客户端天空现在实际渲染血月、丰收月和蓝月的月面颜色，以及血月/蓝月光晕和月晕环；新增的 MITE 资源已加入来源与 SHA-256 追溯清单。
- 月相规则统一为主世界限定与 MITE 夜晚窗口（`13000..23000`），应用到生物、作物、睡眠、雾和天空效果，避免其他维度或白天误触发。
- 钓鱼与天气保留现有的轻量化月相效果，不在本次重写为严格 MITE 实现：前者需要接管咬钩机会、技能、附魔和鱼饵流程，后者需要世界种子驱动的 `WeatherEvent` 调度、预报与合并规则。

### 还原 MITE 露天火焰烹饪

- 掉落的生食现在仅在实际受到非熔岩火焰伤害时累计烹饪进度；每点伤害增加 3 点进度，达到 100 点后转为对应熟食并立即生成 MITE 数值的经验球。熟食继续受到火焰伤害会在同一阈值后烧毁；燃烧生物掉落的熟食会继承 2 至 8 秒火焰。
- 对应食材调整为 MITE 配对：猪、牛、鸡、羊、鳕鱼、鲑鱼、土豆、面团和虫；兔肉不再作为 MITE 露天烹饪对象。鳕鱼/鲑鱼经验调整为 3/4，羊肉为 2，土豆、面团和虫为 0。
- 生食受到火焰伤害后，会在下一个 10 tick 检查同一火格内的实际生食数量：少于 2 件不灭火，2 至 6 件按 `1% × 2^数量` 概率灭火，7 件及以上必定灭火；检查范围与 MITE 火格范围一致。
- 使用最小范围的 `ItemEntity.hurtServer` Mixin 获取真实伤害数值；NeoForge 的公开无敌检查事件不提供伤害量，无法单独实现 MITE 的按伤害累计规则。

### 将地下世界古城洞穴底部提升到 Y=0

- 地下世界大型洞穴的最低边界从 `Y=-20` 提升到 `Y=0`，顶部仍为 `Y=80`；洞穴之外的深板岩、分层内部基岩、地核和高层 MITE 密度洞穴规则保持不变。
- 地下世界专用古城锚点同步从 `Y=4` 提升到 `Y=24`，中央模板的 `city_anchor` 仍位于局部 `Y=24`，因此古城底部继续与大型洞穴最低边界对齐。

### 将地下世界大型洞穴绑定到古城

- 移除按 `16x16` 宏区块独立尝试的地下世界 configured carver；地下世界生成器现在读取已经成功生成的真实古城结构起点，并只围绕这些起点雕刻新大型洞穴，因此没有古城的位置不再额外生成该洞穴。
- 新洞穴继续使用扭曲 cheese 洞室、层状残留、天然柱和稀疏通道，并保留 `Y=-20..80` 边界及地核、边界基岩、矿物和液体保护；高层 MITE 密度洞穴保持不变。
- 新增仅面向地下世界深暗区的 `infx:underworld_ancient_city`，复用原版古城全部拼图与战利品，并把锚点从 `Y=-27` 提升到 `Y=4`；中央模板的 `city_anchor` 位于局部 `Y=24`，因此模板底部正好落在新洞穴底界 `Y=-20`。结构间距和 `beard_box` 适配保持原版，主世界 `minecraft:ancient_city` 定义不改。
- 为防止生物群系分界截断古城空间，洞穴不再按每个方块的深暗生物群系裁切；生成资格仍由真实古城起点保证，古城只会在地下世界深暗区成功起始。

### 重做地下世界深暗大型洞穴

- 深暗大型洞穴不再使用固定主椭球与四个对称侧翼，改为连续的扭曲 cheese 噪声洞室，并叠加层状残留、天然柱和稀疏 spaghetti/noodle 通道；洞穴上下边界与外缘渐进收束，避免规则圆顶、十字轮廓和垂直切面。
- 大型洞穴最低边界调整为 `Y=-20`，顶部保持 `Y=80`；地核、边界基岩、矿物、液体保护和古城局部 `beard_box` 适配保持不变。

### 修复地下世界古城底层积水

- 地下世界噪声填充现在会在 `Y=-54..99` 将负密度位置生成为空气，避免古城地形适配绕过水源高度检查并在底层生成源水；`Y<-54` 熔岩、`Y=120..143` MITE 水层和 `sea_level=144` 保持不变。

### 限制地下世界水源最低高度

- 地下世界水源现在不会生成在 `Y=100` 以下；MITE 密度洞穴的 `Y=120..143` 低层水、`sea_level=144` 和熔岩规则保持不变。

### 调整地下世界分区视觉颜色

- 普通地下世界现在使用墨绿色雾色与环境光色，繁茂区使用暖琥珀色；深暗区、水色、雾距和环境光强度保持不变。

### 修复地下世界装饰区块读取崩溃

- 菌丝 post 和棕色蘑菇装饰现在通过生成器的未缓存噪声生物群系采样判断繁茂区，不再从 `WorldGenRegion` 读取邻区块，避免新区块生成阶段触发 `Requested chunk unavailable during world generation`。

### 修复皮革护甲物品贴图颜色

- 皮革护甲物品模型现在直接引用 Minecraft 26.1 原版皮革模型和材质，不再使用 MITE 皮革贴图；未染色时使用原版默认皮革颜色，仍保留染色组件支持。

### 添加地下世界深暗之域

- 地下世界现在按世界种子低频噪声划分为普通区、繁茂区和深暗区，比例约为 94%、5% 和 1%，并保持垂直方向一致。
- 深暗区生成大型低层矿洞、深暗之域的 sculk 装饰，并加入古城生物群系标签；不加入试炼密室标签。
- 繁茂区与普通/深暗区的植被装饰互斥，原有 MITE 密度地形、矿物、地牢、液体和地下怪物生态保持不变。
- 大型矿洞只围绕地下世界中成功生成的古城结构起点生成，保护地核、边界基岩、矿物、空气和水；已生成区块不会重写。

### 恢复原传送门材质

- 普通地下世界门重新使用原版传送门动画，下界门继续使用既有 MITE 下界门动画；按目的地的门面、粒子和符文门颜色规则保持不变。
- 删除上一项引入的通用传送门底图及其资源同步和来源追溯记录。

### 按目的地重构传送门颜色

- 普通传送门现在按目的地着色：主世界蓝色、地下世界紫色、地狱红色；往返传送时会随目标维度切换。
- 普通传送门、符文门门面、粒子和符文门传送动画共用同一套目的地颜色规则。
- 动态门面使用 MITE 通用传送门动画纹理作为底图，避免跨维度 tint 时保留来源维度的红色色相。

### 修复地下世界回程门错误改型

- 主世界自动生成的地下世界回程门现在始终保持 `underworld_portal` 身份，再次进入时不会因门框不在底层基岩上而被改成返回出生点门；旧门复用也不再重新分类已有的 InfX 专用门面。
- 已经被旧逻辑改成 `return_spawn_portal` 的门不会自动恢复，以免误改正常返生门；需要拆除后从地下世界再次返回以生成正确门面。极旧存档中的共用门面如需其他用途，也需要拆除并重新点火。

### 修复合成经验与工具品质

- 合成金属币现在按产出数量扣除对应经验，硬币拆分为金属粒继续返还输入硬币经验；品质工具也会在合成完成前扣除高品质经验成本。
- 正等级且没有女巫诅咒的玩家不能选择破烂或粗劣工具；负等级会强制产出破烂或粗劣品质，只有女巫诅咒的智力下降会额外降低有效等级。
- 金属币和品质工具的经验消耗现在显示在结果槽 tooltip 中，不再发送品质选择系统消息。

### 降低地下世界繁茂区比例

- 地下世界繁茂区仍使用稳定的 `4x4` 区块分区，但生成概率从约一半降低为每 20 个分区 1 个，约占 5%。
- 繁茂植被、非繁茂区菌丝 post 与棕色蘑菇的规则保持不变；已生成区块不会重写。

### 添加分区式地下世界繁茂洞穴

- 地下世界按世界种子划分为稳定的 `4x4` 区块分区，约 5% 分区在 `Y=144..247` 的高层空气洞穴中生成原版繁茂洞穴植被。
- 繁茂分区使用苔藓、洞穴藤蔓、黏土、繁茂植被、缠根树、孢子花和普通藤蔓；不引入 `minecraft:lush_caves` 生物群系或水生生态。
- 非繁茂分区继续生成原有菌丝 post 和棕色蘑菇；菌丝 post 的跨区块影响不会污染繁茂分区，旧装饰的其他规则保持不变。

### 新增 `/infx xp` 指令

- 玩家执行 `/infx xp` 时可以查看当前总经验值、经验等级和当前等级进度。

### 隐藏非测试模式的额外物品属性

- 非 test 模式下不再显示食物的营养明细或工具/装备的 InfX 扩展属性；test 模式继续保留这些调试提示。

### 移除普通燧石和黑曜石工具台

- 移除普通 `flint_workbench` 和 `obsidian_workbench` 方块、物品及配方；去皮原木工具台变体和对应的燧石/黑曜石等级菜单继续保留。
- JEI、进度和 GameTest 改为使用去皮原木工具台，保留两种工作台顶面材质供变体使用。

### 修复 InfX 狼移速

- 普通 `infx_wolf` 的基础移动速度恢复为原版 `0.30`，避免狼在游戏中移动异常过快；地狱犬和惧狼的 MITE 专属移速不变。

### 移除主世界深暗之域和蘑菇岛

- 主世界气候生物群系源不再生成深暗之域或蘑菇岛；两个原版生物群系注册和其他维度行为保持不变。

### 新增去皮原木工具台

- 为 11 种去皮原木分别新增燧石工具台和黑曜石工具台；侧面与底面使用对应去皮原木材质，顶部沿用原燧石或黑曜石工作台材质。
- 新工具台使用对应去皮原木合成，并自动加入掉落、挖掘标签、便携标签、创造模式物品列表、语言和数据生成资源。

### 修复金属硬币配方

- 恢复铜、银、金、远古金属、秘银和艾德曼币与对应金属粒之间的 MITE 配方；金属粒可以合成硬币，硬币也可以拆回金属粒。

### 修复硬币使用与合成经验返还

- 硬币右键使用后正确同步消耗结果，并立即获得对应经验。
- 使用硬币合成对应金属粒后，按每个被消耗输入槽返还硬币经验。

### 按维度调整符文门颜色

- 符文门门面、粒子和传送动画现在按所在维度着色：主世界为蓝色，地下世界为紫色，下界（地狱）使用与下界门一致的 MITE 红色。
- 为符文门模型补充带 tint 的门面模板，使维度颜色能够正确作用于动态材质。

### 扩展结构战利品并转换原版装备

- 恢复 26.1.2 原版结构箱表，仅继续取消出生箱和 Piglin barter；古城、堡垒遗迹、埋藏宝藏、末地城、雪屋、前哨站、残破传送门、沉船、海底废墟、村庄、林地府邸和试炼密室现在追加 MITE/InfX 奖励。
- 新增 48 个实际结构箱目标的 MITE 奖励池，保留 MITE 权重、滚数和数量范围；试炼密室只修改 `reward` 与 `reward_ominous` 入口，不修改遗迹考古箱表。
- 新增结构战利品 GameTest，实际抽取普通地牢、堡垒遗迹宝藏、村庄铁匠、末地城和试炼密室奖励。
- 原版铁/钻石装备、锁链甲、马铠、长矛和鹦鹉螺甲在战利品过滤阶段转换为远古金属装备，并保留数量、损伤、附魔和名称等兼容组件。
- 新增 MITE 第 10 天、第 20 天和 `Y=48` 战利品门槛；铜/金/铁资源、银资源、碎片、丝线、空碗、洋葱、奶酪和桶按 InfX 映射规则生成。

### 修复 `infx:sgravel` 砾石掉落

- 挖掘 `infx:sgravel` 现在沿用原版砾石的掉落选择，包含相同概率的金属粒及其他掉落分支。
- GameTest 现在直接抽样原版砾石和 `infx:sgravel` 的实际方块状态，验证两者都能进入相同掉落概率范围。

### 修复符文石和金属砧语言

- 修复符文石和金属砧物品错误使用缺失的 `item.infx.*` 描述键，现在沿用对应方块的中英文名称。

### 移除地下世界 InfX 史莱姆自然生成

- 地下世界自然怪物表不再生成 `infx:infx_slime`；其余 19 项怪物、史莱姆变种实体和其他维度的史莱姆规则保持不变。

### 删除大型粘土烤炉

- 删除 `large_clay_oven` 方块、物品、配方、模型、标签、语言条目和相关 GameTest。

### 恢复地下世界 InfX 怪物生态

- 地下世界现在直接生成 20 项 InfX 怪物：基础蜘蛛、苦力怕、史莱姆、末影人，MITE 对应怪物，以及洞穴蜘蛛、长眠者和远古骷髅领主；权重和数量范围按本次地下世界规则固定。
- Longdead 自然生成时有 `1/6` 概率替换为 Longdead Guardian；地牢刷怪笼、指令、刷怪蛋和结构生成不受替换影响。
- 新增 `infx:longdead_guardian` 的属性、远古金属装备、战利品、标签、渲染和刷怪蛋；不将 Guardian 加入地下世界自然权重表。
- 地下世界继续排除 MITE 明确移除的僵尸、骷髅、食尸鬼、亡魂和 Bone Lord，以及没有地下世界自然生成依据的其他 InfX 怪物；非怪物生成表保持现状。

### 恢复 26.1.2 原版物品的 MITE 堆叠上限

- 为原版独立物品和非同名放置物补齐 MITE 堆叠规则：普通物品为 16，种子、纸、金属粒和碎片类保留 64，钻石与绿宝石为 32，锭、砖、玻璃瓶和空桶为 8。
- 将碗装食物设为 4、完成书设为 1，并保留横幅的 16 堆叠上限。

### 调整下界顶部与底部地层

- 下界最上方一层基岩现在改为地幔，最下方一层改为一层地核；其余原版随机基岩层和地形规则保持不变。
- 该调整仅作用于之后新生成的下界区块，已生成区块不会重写。

### 地幔和地核踩踏着火

- 非火焰免疫实体踩上地幔或地核时会立即着火八秒。

### 统一未设置堆叠上限的物品

- 将 InfX 普通食物、面粉、凝胶球、刷怪蛋、Catalog 原料、Catalog 箭以及原版骨头的最大堆叠数量统一设为 16。

### 恢复地下世界自然装饰

- 地下世界新生成区块现在会进行 70 次 MITE 风格液体源尝试：候选高度保留原始的低层偏置，且只有顶底为石头、四侧恰有三块石头和一格空气的洞穴缝隙才会生成水或熔岩。
- 每个地下世界装饰区块保留一次 `1/4` 概率的棕色蘑菇候选，候选坐标采用原始区块随机坐标加 `8`，高度为 `Y=120` 至 `247`。
- 新增按世界种子稳定的菌丝 post 场：每个候选区块以 `1/16` 概率产生一个 post，扫描周边 `5×5` 候选区块并在半径 24 格内生成菌丝和少量棕色蘑菇；实现不使用跨区块可变缓存，因此不会因并行生成顺序产生接缝。

### 恢复地下世界地牢装饰

- 地下世界新生成区块现在在地下结构装饰阶段进行 16 次 MITE 风格地牢候选，候选位置为区块内随机坐标加 `8`，高度为 `Y=140` 至 `171`；洞穴入口、地板和顶板仍按原版怪物房规则校验。
- 地牢刷怪笼以 `5/6` 概率生成长眠者、以 `1/6` 概率生成远古骷髅领主，并直接绑定现有的地下世界专属简化战利品表；自然生物表保持为空。
- 删除了此前追加到原版普通地牢箱的全局地下世界战利品修改器，避免其他维度的普通地牢获得地下世界战利品。

### 修正传送门互联

- 普通地下世界门和下界门现在按当前实体坐标与维度坐标比例寻找出口，不再读取或写入玩家级旧返回坐标；符文门的远距目标改为入口坐标叠加符文确定性偏移，避免移动符文门后从下界返回到旧地下世界位置。

### 隔离测试模式服务端权限

- `-Dinfx.testMode=true` 时保留原版 OP、`ops.json`、本地控制台、RCON 与 JSON-RPC；测试客户端只能连接到同为测试模式的服务端。
- 普通模式下玩家不再获得 OP 或 OP 满服绕过，已有 `ops.json` 保留但不生效，新建专用服不再自动生成空 `ops.json`。
- 普通模式下本地终端和 GUI 控制台仅允许受限命令白名单，其他命令不执行；RCON 与 JSON-RPC 不会启动监听器。

### 更新地幔材质

- 地幔方块现在内置并使用用户提供的 `16×48` 三帧动画材质，不再引用原版岩浆材质。

### 调整传送门粒子颜色

- 进入下界的传送门现在发出 MITE 红色粒子；普通地下世界门继续使用紫色粒子。
- 符文门和回出生地门现在发出与其青蓝色门面一致的粒子。

### 更新下界传送门材质

- 进入下界的 `infx:nether_portal` 现在使用所提供的 MITE 红色动画传送门材质；地下世界传送门和返生传送门不受影响。

### 调整深层传送门

- 主世界门框底边位于世界底部起八格内且至少一格底框下方接触基岩时，可在当前主世界约 `Y=-10` 的深度进入地下世界；地下世界同等深度的地幔支撑门框可进入下界。
- 地下世界新生成区块的随机底部边界改为地幔，使其能够自然满足进入下界的门框条件；已生成区块不会重写。
- 地下世界与下界门现在支持宽 `2` 至 `21`、高 `3` 至 `21` 的完整门框；新建出口会继承入口门的宽度、高度与朝向，而不再固定生成 `2x3` 门。

### 恢复地下世界内部基岩层

- 地下世界深板岩基底现在叠加四层按世界种子稳定的 MITE 风格二维噪声基岩，中心从低到高为 `Y=-120`、`-72`、`-16` 和 `99`。
- 第一层保留向下连通的封闭形态，其余三层保留不规则上下边缘；仅替换 `Y=-128` 至 `119` 的深板岩，因此不会改写随机地核、MITE 密度洞穴、低层水或顶部基岩。

### 新增已修改结构清单

- 在 `docs/modified-structures.md` 汇总结构生成门禁、结构集与生物群系资格、生成后处理和结构战利品修改。

### 恢复地下世界 MITE 密度地形与深层地层
### 新增结构生成进度门禁

- 林地府邸现在要求任意一名在线玩家当前经验达到 100,000 点；经验被消费、默认死亡导致经验下降或玩家离线后，若没有其他玩家满足阈值，府邸会重新锁定。
- 海底神殿要求任意玩家进入过下界要塞；六种主世界残破传送门要求任意玩家进入过下界；沉船和搁浅沉船要求任意玩家击杀过守卫者。
- 掠夺者前哨站现在与村庄共享第 60 天且全世界已制作铁级工具的生成条件。

### 移除古城与试炼密室生成

- 新生成的主世界区块不再生成远古城市或试炼密室；原版注册表和已生成区块不受影响。

### 重写地下世界深层地层

- 地下世界的新区块从 `Y=-128` 延伸至 `Y=255`，总高 384 格；`Y=-128` 至 `Y=119` 使用深板岩，底部从 `Y=-128` 起由按位置和世界种子稳定的随机 1 至 5 层 `infx:core` 覆盖。
- `Y=120` 至 `Y=247` 恢复整体上移 120 格的 MITE 旧 Nether 密度地形：正密度生成石头，洞穴在 `Y=120` 至 `Y=143` 填水、在 `Y=144` 至 `Y=247` 保持空气。
- `Y=248` 至 `Y=250` 保持石头；顶部随机 1 至 5 层基岩、空生物表、关闭的矿物/雕刻器/放置特征与旧随机源保持不变。
- 已生成区块不会重写，旧区块与新生成区块的边界可能出现地层断面。

### 添加地下世界随机顶底边界

- 新生成地下世界区块的底部会生成随机 1 至 5 层地核，`Y=0` 始终为地核。
- 顶部会生成随机 1 至 5 层基岩，`Y=255` 始终为基岩；这两种边界均按世界种子与位置稳定生成。
- 已生成区块不会重写。

### 调整地下世界高度

- 地下世界现在从 `Y=0` 延伸到最高可放置方块 `Y=255`（上边界为 `Y=256`），共 256 格高度。

### 统一资源文件前缀

- 将以 mite 或 r196 开头的受跟踪资源文件名统一替换为小写 infx（Minecraft 资源标识符要求小写），并同步资源清单、同步脚本和校验测试。

### 统一 Java 与注册前缀

- 将以 Mite 或 R196 开头的 Java 文件名和类型改为 Infx，并同步引用、注册常量、注册路径、数据生成逻辑和测试资源路径；保留旧 NBT 键以兼容已有存档。

### 恢复主世界人工矿井

- 主世界重新使用原版普通与恶地人工矿井的生物群系分布；地下世界不再生成这两类矿井。
- 该调整仅影响之后新生成的区块，已探索区块不会补建或重写人工矿井。
### 重置地下世界生成基线

- 新生成的地下世界区块不再应用洞穴密度、地表替换、雕刻器、矿物与其他放置特征，也不再生成 MITE 内部基岩层或顶底边界基岩；整个有效高度范围默认填充为石头。
- 地下世界不再被标记为古城、埋藏的宝藏、废弃矿井、古迹废墟或试炼密室的生成生物群系；已生成区块不会被重写。
- 地下世界现在完全清空生物生成表，不再自然生成任何原版或 InfX/MITE 生物。

### 新增结构生成进度框架

- 新增可复用的结构生成规则注册表，支持按结构 ID 或结构标签、维度和可组合的世界进度条件限制新生成区块。
- 村庄现改由该框架控制：主世界第 60 天且世界首次制作铁级工具后才会在后续新生成区块中出现；既有区块不会回填。

### 调整地下世界高度

- 地下世界现在使用 `Y=-128` 至上边界 `Y=192` 的 320 格高度范围，最高可放置方块为 `Y=191`；维度类型与噪声设置会同步使用该范围。

### 重置主世界资源矿物生成

- 主世界的煤、铜、铁、银、金、秘银、红石、钻石、青金石和绿宝石现在使用新的硬核稀缺矿表；银和秘银会按石头、深板岩宿主生成对应矿石。
- 移除了原版资源矿物的生物群系额外生成，并关闭原版铁、铜噪声矿脉；地下地形变种、虫蚀石和地下世界矿物保持不变。
- 新规则只会作用于之后新生成的主世界区块，不会改写已探索区块。

### 修复不可挖掘方块与金属铁砧回收

- 服务端现在会拒绝无法产生破坏进度的起挖输入，使其按空挥处理，并避免错误开启挖掘代谢。
- 工具类型不匹配或采集等级不足的方块也会按空挥处理，不能再开始挖掘。
- 客户端现在会将上述无效方块目标走原版左键空气分支，实际播放一次空挥而非仅取消起挖。
- 金属铁砧恢复可手持回收：保留 128 tick 手挖速度、掉落铁砧物品并保留已有损耗。

### 新增砾石方块

- 新增使用用户提供材质的砾石方块；只有燧石铲及更高采掘等级的铲类工具可以正确挖掘。
- 将砾石方块注册名从 `infx:gravel` 更正为 `infx:sgravel`。

### 调整山地表层生成

- 裸岩山峰和风袭沙砾山丘不再生成原版沙盘或砾石盘，改为生成 `infx:sgravel` 砾石盘。
- 风袭沙砾山丘由噪声地表规则生成的原版砾石也改为 `infx:sgravel`。

### 调整水边地表生成

- 石岸和河流底的原版沙盘、砾石盘及地表规则砾石改为生成 `infx:sgravel`。
- 修复石岸和河流砾石盘与山地特征共享放置对象导致的地物排序环。

### 调整砾石斑块生成

- 裸岩山峰、风袭沙砾山丘、石岸和河流中的原版砾石斑块现在也会生成 `infx:sgravel`。

### 调整经验掉落

- 动物死亡和挖掘煤矿、深层煤矿后不再掉落经验。
### 修复工作台合成进度箭头

- 合成进度箭头左移 1 像素，使白色填充逐像素对齐工作台背景箭头。
- 工作台和背包合成进度现在会在服务端刻之间平滑过渡，长配方不再逐像素停顿跳动。
- 合成完成时进度会短暂显示满格后再清零；暂停和取消仍会准确停住或立即复位。

### 添加深板岩矿物

- 添加银、秘银和艾德曼深板岩矿石及其用户提供的材质。
- 三种深板岩矿石沿用对应石头矿石的采掘等级，暂不加入世界生成。

### 调整巨型吸血蝙蝠体积

- 吸血巨蝠现在缩小为普通吸血蝙蝠的 1.5 倍，仍保留更大的渲染和碰撞体积。

### 更新蝙蝠材质

- 夜翼和吸血蝙蝠现在使用用户提供的 32×32 实体材质。
### 修复玩家背包合成界面

- 玩家背包合成现在显示定时合成进度条。
- 背包合成进度现在沿原版箭头轮廓填充，不再出现放大、越界或变形的白色箭头。
- 定时合成完成后的产物现在通过原版同步入包流程交付，不再只清空进度而客户端看不到物品。

### 修复 MITE 土元素攻城 AI

- 土元素现在使用独立的攻城挖掘目标，按 MITE 的目标支撑方块、上半身遮挡和下半身遮挡顺序选取可破坏方块。
- 连续破坏会保留 MITE 的初始冷却、材质速度、短暂停顿和重新选块逻辑；可到达目标、关闭 `mobGriefing` 或目标失效时会正确取消破坏进度。

### 修复土元素实体继承

- 土元素和黏土元素现在直接使用敌对生物基类，不再继承铁傀儡的村庄守卫、修复和碰撞行为。
- 土元素和黏土元素受击和死亡时恢复播放 MITE 的铁傀儡音效。

### 对齐 MITE R196 蝙蝠

- 普通蝙蝠现在使用 3 点生命的 R196 实体，并替换原版自然生成、刷怪笼替换和环境生成表。
- 蝙蝠恢复 MITE 的环境生物身份、洞穴空气与区块光照生成规则、集群上限，以及正常蝙蝠 0 经验和无掉落。
- 吸血蝙蝠和夜翼现在以视线内的猎物为目标；吸血蝙蝠不会捕食地狱犬，满血冷却期间只攻击玩家，并会触发受伤豹猫反击。
- 夜翼不再施加现代黑暗效果，改为 MITE 的玩家专属屏幕渐暗；银甲覆盖率会降低该效果。

### 修复 R196 GameTest 回归

- 便携方块的手持挖掘速度现在补偿原版的非正确工具 `/100` 分母；保险箱所有者重新保持固定的 128 tick 回收速度，并能正常收回保险箱物品。
- 行为饥饿 GameTest 现在覆盖已移除的自定义无效挖掘拦截：输入继续交由原版处理，同时持续挖掘仍按现有规则结算代谢。
- 村民转换 GameTest 显式使用空手 R196 僵尸，避免装备状态干扰其 MITE 转换前提。
- 女巫诅咒 GameTest 直接验证实体级无敌的生存玩家仍可进入诅咒候选集，不再把 1/4 的游戏内随机施咒概率当作测试前提。
- 骷髅重新评估武器时会停止并移除旧的原版及 R196 弓攻击 goal，避免遗留冷却和重复 goal 抢占当前武器行为。
- 远程攻击距离 GameTest 现在强制动态实体区块保持 ticking，并为骷髅装备头盔，避免区块卸载或明亮世界中的避日寻路干扰弓攻击断言。
- 末影人珍珠拾取 GameTest 现在强制实体区块 ticking、屏蔽同批测试创建的无关玩家目标，并保护测试珍珠不被其他玩家拾取。

### 对齐 MITE 普通刷怪笼光照

- 普通刷怪笼现在忽略火把和其他环境亮度造成的现代黑暗判定，恢复 MITE 1.6 行为。
- 非光照出生条件仍保留；会在露天白天被日光灼伤的实体仍不会由刷怪笼生成。试炼刷怪笼和自定义 SpawnRules 不受影响。
### 调整丛林植被

- 丛林、稀疏丛林和竹林不再自然生成西瓜。

### 修复疾跑

- 营养值耗尽时会立即停止疾跑，剩余饱和度不再允许重新疾跑。
### 修复测试模式世界类型

- test mode 创建和重新载入世界时会采用所选的超平坦等世界类型，不再被巨型生物群系配置覆盖。
- 普通模式仍保持现有世界类型锁定规则。

### 修复材料剪刀

- 材料剪刀现在可正确获得精准采集。
- 右键材料剪刀可直接剪切树叶、羊毛、蓝莓丛等剪刀有效方块。
- 右键剪切方块现在播放剪刀音效，并使蓝莓丛同时掉落丛本体。
- 左键使用材料剪刀不再破坏方块。
- 左键材料剪刀恢复正常近战攻击。
- 副手剪刀不再误用主手武器执行右键攻击。
- 被事件取消的剪刀攻击不再消耗耐久或施加冷却。

### 修复主世界地形

- 移除主世界大型洞穴生成，避免海底出现异常水幕和瀑布。
# InfiniteX 更新日志

## 未发布 - 2026-07-29

- 恢复地下世界矿物生成：按 MITE 的低层/全高数量在石头和深板岩中生成铜、银、金、铁、秘银、精金、红石、钻石、青金石、银鱼石和有支撑的砾石，并保护地核、基岩、空气与液体。
- 删除战斗断线生命/生存数据惩罚和非正常断线后的重连时间限制。
- 删除聊天刷屏计分与自动踢出。
- 删除 InfiniteX 自定义无效挖掘输入和采掘合法性拦截，保留 MITE 挖掘速度计算与保险箱所有权保护。
